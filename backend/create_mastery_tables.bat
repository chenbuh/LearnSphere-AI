mysql -u root -pchen20040209 learnsphere_ai -e "CREATE TABLE IF NOT EXISTS vocabulary_mastery (id BIGINT AUTO_INCREMENT PRIMARY KEY, user_id BIGINT NOT NULL, vocabulary_id BIGINT NOT NULL, mastery_level TINYINT DEFAULT 0, review_count INT DEFAULT 0, correct_count INT DEFAULT 0, wrong_count INT DEFAULT 0, last_review_time DATETIME, next_review_time DATETIME, is_favorite BOOLEAN DEFAULT FALSE, first_learned_time DATETIME, notes TEXT, deleted TINYINT DEFAULT 0, create_time DATETIME DEFAULT CURRENT_TIMESTAMP, update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, UNIQUE KEY uk_user_vocab (user_id, vocabulary_id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;"

mysql -u root -pchen20040209 learnsphere_ai -e "CREATE TABLE IF NOT EXISTS daily_learning_stats (id BIGINT AUTO_INCREMENT PRIMARY KEY, user_id BIGINT NOT NULL, stat_date DATE NOT NULL, vocabulary_count INT DEFAULT 0, grammar_count INT DEFAULT 0, reading_count INT DEFAULT 0, listening_count INT DEFAULT 0, writing_count INT DEFAULT 0, speaking_count INT DEFAULT 0, total_time_minutes INT DEFAULT 0, total_questions INT DEFAULT 0, correct_questions INT DEFAULT 0, create_time DATETIME DEFAULT CURRENT_TIMESTAMP, update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, UNIQUE KEY uk_user_date (user_id, stat_date)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;"

echo Done! Tables created successfully.
pause
