# LearnSphere AI 组件开发规范

> 本文档定义了项目中的组件开发规范，确保代码质量和一致性。

## 📋 组件开发清单

在创建新组件前，请确保：

- [ ] 组件用途明确，职责单一
- [ ] 遵循命名规范（PascalCase）
- [ ] 使用 Composition API (`<script setup>`)
- [ ] 定义清晰的 Props 和 Emits
- [ ] 添加必要的类型注解
- [ ] 编写组件注释说明
- [ ] 实现响应式设计
- [ ] 添加加载和错误处理
- [ ] 编写基础的单元测试

## 🎯 组件结构模板

```vue
<template>
  <div class="component-name">
    <!-- 模板内容 -->
  </div>
</template>

<script setup>
/**
 * 组件说明
 * @component ComponentName
 * @description 组件的详细描述
 * @author 开发者名称
 * @date 创建日期
 */

// 1. 导入依赖
import { ref, computed, onMounted, onUnmounted } from 'vue'
import { useRouter } from 'vue-router'
import { NIcon, NButton } from 'naive-ui'
import { IconName } from 'lucide-vue-next'

// 2. 定义 Props（带类型和默认值）
const props = defineProps({
  title: {
    type: String,
    required: true,
    default: ''
  },
  count: {
    type: Number,
    default: 0
  },
  disabled: {
    type: Boolean,
    default: false
  }
})

// 3. 定义 Emits
const emit = defineEmits(['update', 'delete', 'change'])

// 4. 响应式状态
const isLoading = ref(false)
const dataList = ref([])

// 5. 计算属性
const hasData = computed(() => dataList.value.length > 0)

// 6. 方法
const fetchData = async () => {
  isLoading.value = true
  try {
    // API 调用
  } finally {
    isLoading.value = false
  }
}

const handleClick = () => {
  emit('update', props.count + 1)
}

// 7. 生命周期
onMounted(() => {
  fetchData()
})

// 8. 清理
onUnmounted(() => {
  // 清理定时器、事件监听等
})
</script>

<style scoped>
.component-name {
  /* 组件样式 */
}
</style>
```

## 📝 Props 定义规范

### 基础类型
```javascript
const props = defineProps({
  // 必需字符串
  title: {
    type: String,
    required: true
  },
  // 可选数字（带默认值）
  count: {
    type: Number,
    default: 0
  },
  // 可选布尔
  disabled: {
    type: Boolean,
    default: false
  },
  // 数组
  items: {
    type: Array,
    default: () => []
  },
  // 对象
  config: {
    type: Object,
    default: () => ({})
  }
})
```

### 复杂类型
```javascript
// 自定义验证
const props = defineProps({
  // 枚举类型
  size: {
    type: String,
    default: 'medium',
    validator: (value) => ['small', 'medium', 'large'].includes(value)
  },
  // 函数类型
  callback: {
    type: Function,
    default: () => {}
  }
})
```

## 🎨 样式编写规范

### 命名规范
```css
/* ✅ 使用 kebab-case */
.component-name { }
.user-avatar { }
.button-primary { }

/* ❌ 避免使用 camelCase 或 PascalCase */
.userName { }
.ButtonPrimary { }
```

### 样式作用域
```vue
<style scoped>
/* 组件内部样式 - 使用 scoped */
.component-name { }
</style>

<style>
/* 全局样式 - 不使用 scoped */
:deep(.global-class) { }
</style>
```

### 响应式断点
```css
/* 移动端适配 */
@media (max-width: 768px) {
  .component-name {
    /* 移动端样式 */
  }
}
```

## 🔧 性能优化规范

### 组件懒加载
```javascript
// ✅ 使用 defineAsyncComponent
const HeavyComponent = defineAsyncComponent(() =>
  import('./HeavyComponent.vue')
)
```

### 列表优化
```vue
<!-- ✅ 使用 key -->
<div v-for="item in list" :key="item.id">
  {{ item.name }}
</div>

<!-- ✅ 使用 v-once 静态列表 -->
<div v-for="item in staticList" :key="item.id" v-once>
  {{ item.name }}
</div>
```

### 计算属性缓存
```javascript
// ✅ 使用 computed 缓存计算结果
const filteredList = computed(() => {
  return list.value.filter(item => item.active)
})

// ❌ 避免在模板中直接调用方法
<div v-for="item in filterList(list)">  <!-- ❌ -->
```

## 🧪 测试规范

### 组件测试模板
```javascript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import ComponentName from '@/components/ComponentName.vue'

describe('ComponentName', () => {
  it('正确渲染组件', () => {
    const wrapper = mount(ComponentName, {
      props: {
        title: 'Test'
      }
    })

    expect(wrapper.find('.component-name').exists()).toBe(true)
  })

  it('正确处理点击事件', async () => {
    const wrapper = mount(ComponentName)

    await wrapper.find('.button').trigger('click')

    expect(wrapper.emitted('update')).toBeTruthy()
  })
})
```

## 📚 相关文档

- [开发指南](../开发指南/开发指南.md)
- [API 文档](../开发指南/接口文档.md)
- [调试指南](../开发指南/调试指南.md)
