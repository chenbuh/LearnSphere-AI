# ⚡ 快速修复：移除Git中的敏感配置文件

## 📋 执行步骤

### 1️⃣ 从Git跟踪中移除敏感文件（必须执行）

```powershell
# 在项目根目录执行
cd "e:\Project\LearnSphere  AI"

# 移除敏感文件的Git跟踪
git rm --cached backend/src/main/resources/application.properties
git rm --cached backend/src/main/resources/application-dev.yml
git rm --cached backend/src/main/resources/application-prod.yml

# 提交更改
git commit -m "chore: Remove sensitive configuration files from version control"
```

### 2️⃣ 验证.gitignore生效

```powershell
# 检查这些文件是否不再被追踪
git status

# 应该看到这些文件显示为 "untracked" 或不显示
# 如果显示为 "Changes to be committed"，说明还在追踪中
```

### 3️⃣ 更新API密钥

**重要：立即更换您的阿里云API密钥！**

1. 访问阿里云控制台：https://dashscope.console.aliyun.com/
2. 删除旧密钥：`sk-8b5df7e3d85442fd8e4ddb7c5204da48`
3. 生成新密钥
4. 更新 `backend/src/main/resources/application-secret.properties` 中的 `ai.api-key`

### 4️⃣ 如果代码已推送到远程仓库

**选项A：强制更新远程仓库（简单但有风险）**

```powershell
# ⚠️ 警告：这会覆盖远程仓库！
# 只在您是唯一开发者或团队已协商的情况下执行

git push origin --force main  # 或 master，取决于您的主分支名
```

**选项B：清理Git历史（彻底但复杂）**

参考 `docs/SECURITY_GUIDE.md` 中的详细步骤

**选项C：删除并重建仓库（最安全但最麻烦）**

1. 在平台上删除远程仓库
2. 创建新仓库
3. 推送清理后的代码

---

## ✅ 配置验证

确保应用能正常运行：

```powershell
# 启动后端
cd backend
mvn spring-boot:run

# 检查日志，确保没有配置错误
```

---

## 🔒 长期安全建议

1. **定期检查**: 每月运行 `git grep -i "sk-"` 搜索可能的密钥泄露
2. **启用预提交钩子**: 使用 git-secrets 等工具自动检测
3. **代码审查**: 在PR中特别注意配置文件更改
4. **权限控制**: 限制谁能访问生产环境配置

---

详细说明请查看：`docs/SECURITY_GUIDE.md`
