# LearnSphere AI 数据架构与持久化建议

## 📊 当前架构问题分析

### 现状
1. **前端混用 LocalStorage**：部分学习进度可能存在浏览器本地
2. **后端有 MySQL**：用户、词汇、语法等数据已持久化
3. **Redis 刚启用**：目前仅用于 VIP 配额计数
4. **数据不同步**：用户换设备后学习进度丢失

### 痛点
- ❌ 用户换设备/浏览器后进度归零
- ❌ 无法多端同步（手机/电脑）
- ❌ 数据不可靠（清除浏览器缓存即丢失）
- ❌ 无法做数据分析和推荐

---

## 🎯 目标架构：云端为主 + 本地缓存

### 核心原则
1. **Single Source of Truth**：MySQL 是唯一数据源
2. **Redis 做缓存**：热点数据、实时数据
3. **前端只缓存**：提升体验，定期同步

---

## 🏗️ 分层架构设计

```
┌─────────────────────────────────────────────┐
│              前端 (Vue 3)                    │
│  ┌────────────────────────────────────────┐ │
│  │  Pinia Store (内存状态)               │ │
│  │  ↓                                      │ │
│  │  LocalStorage (离线缓存，可选)        │ │
│  └────────────────────────────────────────┘ │
│              ↕ HTTP API                      │
└─────────────────────────────────────────────┘
                    ↕
┌─────────────────────────────────────────────┐
│           后端 (Spring Boot)                │
│  ┌─────────────┐      ┌──────────────────┐ │
│  │   Redis     │ ←──→ │  Service 层      │ │
│  │  (缓存层)   │      │  (业务逻辑)      │ │
│  └─────────────┘      └──────────────────┘ │
│         ↓                      ↓             │
│  ┌─────────────────────────────────────────┐│
│  │         MySQL (持久化层)                ││
│  │  [用户] [学习记录] [词汇] [VIP]...     ││
│  └─────────────────────────────────────────┘│
└─────────────────────────────────────────────┘
```

---

## 📦 数据分类与存储策略

### 1. 用户数据（MySQL）
**表：`user`**
- 基本信息：username, email, nickname, avatar
- VIP 信息：vip_level, vip_expire_time, daily_ai_quota
- 统计数据：consecutive_days, total_checkin_days

**存储策略：**
- ✅ 仅存 MySQL
- ✅ 登录后一次性获取，前端缓存到 Pinia
- ✅ 修改后立即写入后端

---

### 2. 学习数据（MySQL + Redis）

#### 2.1 学习记录（`learning_record`）
**存储内容：**
- 做题记录、错题、掌握程度、复习时间

**存储策略：**
```
MySQL (主存储)
  ↓
每次做题后：前端 → POST /api/learning/record → 后端写入 MySQL
  ↓
查询时：GET /api/learning/records?userId=xxx
```

**Redis 缓存（可选优化）：**
```redis
# 缓存用户今日学习进度（避免频繁查库）
Key: "learning:today:{userId}"
Value: { "vocabularyCount": 50, "grammarCount": 10, ... }
TTL: 24 小时
```

#### 2.2 词汇掌握度（`vocabulary_mastery`）
新建表：记录用户对每个单词的掌握情况

```sql
CREATE TABLE vocabulary_mastery (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  vocabulary_id BIGINT NOT NULL,
  mastery_level TINYINT DEFAULT 0 COMMENT '掌握等级 0-5',
  review_count INT DEFAULT 0 COMMENT '复习次数',
  last_review_time DATETIME,
  next_review_time DATETIME COMMENT '下次复习时间（基于遗忘曲线）',
  is_favorite BOOLEAN DEFAULT FALSE,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_vocab (user_id, vocabulary_id),
  INDEX idx_next_review (user_id, next_review_time)
) ENGINE=InnoDB COMMENT='用户词汇掌握度';
```

**数据流：**
```
用户背单词 → 前端记录答对/答错 → POST /api/vocabulary/review
                                    ↓
            后端更新 mastery_level 和 next_review_time（艾宾浩斯曲线）
                                    ↓
                              写入 MySQL
```

---

### 3. 静态内容（MySQL + Redis 缓存）

#### 词汇库 (`vocabulary`)
- **存储**：MySQL
- **缓存策略**：
  ```redis
  # 按考试类型缓存词汇列表（1小时过期）
  Key: "vocab:cet4:page1"
  Value: [{"id":1,"word":"abandon",...}, ...]
  TTL: 3600 秒
  ```

#### 语法题库 (`grammar_exercise`)
- **存储**：MySQL
- **缓存策略**：同上

---

### 4. 实时/临时数据（Redis）

#### 4.1 VIP 配额（已实现）
```redis
Key: "quota:user:{userId}:{date}"
Value: 已用次数
TTL: 1 天
```

#### 4.2 在线学习状态（推荐新增）
```redis
# 记录用户当前正在学习的模块
Key: "session:{userId}"
Value: {"module": "vocabulary", "progress": 50}
TTL: 30 分钟（无操作自动清除）
```

#### 4.3 排行榜（推荐新增）
```redis
# 使用 Redis Sorted Set
Key: "leaderboard:checkin"
Score: total_checkin_days
Member: userId

# 查询 Top 10
ZREVRANGE leaderboard:checkin 0 9 WITHSCORES
```

---

## 🔄 数据同步方案

### 方案 A：实时同步（推荐）
**适用场景**：做题、背单词等高频操作

```javascript
// 前端示例
const submitAnswer = async (answer) => {
  // 1. 本地立即更新（乐观更新）
  store.updateProgress()
  
  // 2. 异步发送到后端
  try {
    await api.submitLearningRecord(answer)
  } catch (error) {
    // 3. 失败则回滚本地状态
    store.rollbackProgress()
    message.error('保存失败，请重试')
  }
}
```

### 方案 B：批量同步（可选）
**适用场景**：离线做题、网络不稳定

```javascript
// 前端缓存多条记录
const offlineQueue = []

// 定时或恢复网络时批量上传
const syncOfflineData = async () => {
  if (offlineQueue.length > 0) {
    await api.batchSubmitRecords(offlineQueue)
    offlineQueue.length = 0
  }
}
```

### 方案 C：LocalStorage 作为降级（不推荐作为主存储）
```javascript
// 仅在网络完全不可用时使用
if (!navigator.onLine) {
  localStorage.setItem('backup', JSON.stringify(data))
}

// 恢复网络后立即同步
window.addEventListener('online', syncOfflineData)
```

---

## 💾 持久化最佳实践

### MySQL 优化

#### 1. 分表策略（学习记录表）
当 `learning_record` 超过 1000 万行时，按时间分表：

```sql
-- 按月分表
learning_record_202601
learning_record_202602
...
```

#### 2. 索引优化
```sql
-- 用户学习记录查询（热点）
CREATE INDEX idx_user_time ON learning_record(user_id, create_time DESC);

-- 词汇掌握度查询
CREATE INDEX idx_user_next_review ON vocabulary_mastery(user_id, next_review_time);
```

#### 3. 定期归档
```sql
-- 6个月前的学习记录移到归档表
INSERT INTO learning_record_archive 
SELECT * FROM learning_record 
WHERE create_time < DATE_SUB(NOW(), INTERVAL 6 MONTH);

DELETE FROM learning_record 
WHERE create_time < DATE_SUB(NOW(), INTERVAL 6 MONTH);
```

---

### Redis 优化

#### 1. 内存控制
```yaml
# redis.conf
maxmemory 2gb
maxmemory-policy allkeys-lru  # LRU 淘汰
```

#### 2. 持久化配置
```yaml
# 同时开启 RDB 和 AOF（数据安全）
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec
```

#### 3. Key 命名规范
```
业务:类型:标识:时间
例如：
  quota:user:1:2026-01-15
  vocab:cet4:page:1
  session:user:123
```

---

## 🚀 实施步骤（渐进式迁移）

### 阶段 1：核心功能云端化（2 周）

#### Week 1：学习记录同步
1. ✅ 创建 `vocabulary_mastery` 表
2. ✅ 实现背单词记录 API
   - `POST /api/vocabulary/review` - 提交单词复习
   - `GET /api/vocabulary/mastery` - 获取掌握情况
3. ✅ 前端改造：做题后立即调用 API

#### Week 2：用户数据完整性
1. ✅ 清理 LocalStorage 中的重复数据
2. ✅ 用户登录后自动拉取云端进度
3. ✅ 实现"同步进度"功能

---

### 阶段 2：性能优化（1 周）

#### Redis 缓存层
1. ✅ 词汇列表缓存（减少 80% 数据库查询）
2. ✅ 今日学习统计缓存
3. ✅ 排行榜使用 Sorted Set

---

### 阶段 3：高级功能（按需）

#### 多端同步
1. ✅ WebSocket 实时推送（可选）
2. ✅ 数据版本冲突检测

#### 离线模式
1. ✅ Service Worker + IndexedDB
2. ✅ 网络恢复后自动同步

---

## 📋 快速检查清单

### 当前项目检查
- [ ] 前端是否还在用 LocalStorage 存学习进度？
- [ ] 用户换设备后数据是否丢失？
- [ ] 是否有"同步失败"的用户投诉？

### 优先级排序
1. **P0（必须）**：学习记录云端化
2. **P1（重要）**：Redis 缓存静态内容
3. **P2（优化）**：离线模式

---

## 🎯 推荐实施方案

### 最小可行方案（1 天实现）
```javascript
// 1. 前端每次做题后立即保存
const submitAnswer = async (answer) => {
  await api.post('/api/learning/record', {
    contentType: 'vocabulary',
    contentId: wordId,
    isCorrect: answer.isCorrect,
    timeSpent: answer.timeSpent
  })
}

// 2. 用户登录后加载进度
const loadUserProgress = async () => {
  const progress = await api.get('/api/learning/progress')
  store.setProgress(progress.data)
}
```

### 完整方案（2 周实现）
按上述"阶段 1 + 阶段 2"执行。

---

## 💡 关键建议

1. **不要过度依赖 LocalStorage**
   - 只用于性能优化（缓存）
   - 绝不作为唯一数据源

2. **设计容错机制**
   ```javascript
   // 保存失败时重试 3 次
   const saveWithRetry = async (data, maxRetries = 3) => {
     for (let i = 0; i < maxRetries; i++) {
       try {
         return await api.save(data)
       } catch (error) {
         if (i === maxRetries - 1) throw error
         await sleep(1000 * (i + 1))
       }
     }
   }
   ```

3. **监控数据同步状态**
   - 前端显示"已同步"/"同步中"/"同步失败"
   - 后台监控同步成功率

4. **MySQL vs Redis 选择原则**
   - **MySQL**：需要永久保存、需要复杂查询
   - **Redis**：临时数据、实时排行、高频读写

---

## 📊 预期效果

实施后：
- ✅ 用户数据 **100% 可靠**（云端存储）
- ✅ 多端同步 **实时生效**
- ✅ 数据库压力 **降低 60%**（Redis 缓存）
- ✅ 用户体验 **大幅提升**（离线可用）

---

需要我帮你实现其中的某个模块吗？（比如"词汇掌握度表 + API"）
