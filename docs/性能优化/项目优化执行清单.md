# 项目优化执行清单（持续更新）

更新时间：2026-02-27
维护规则：每完成一个任务，立即标记为 `[x]`，并在“完成记录”追加一条。

## 完成记录
- [x] 2026-02-27: ECharts loading optimized in frontend views. Replaced direct `echarts` imports with on-demand `@/utils/echarts` core registration (`Bar/Line/Radar` only). Build passed, tests passed, and `echarts-vendor` dropped from about 909 kB to about 358 kB.
- [x] 2026-02-27: Frontend chunking optimization continued. Removed forced Naive UI manual chunk to let Rollup auto-split UI modules, avoiding circular chunk warnings while keeping build/test green.
- [x] 2026-02-27: Frontend validation completed without backend startup (`npm test` passed: 2 files / 6 tests, `npm run build` passed, chunk split remains stable).
- [x] 2026-02-27: Checklist re-verified. No unchecked items remained. Local validation was blocked by missing Maven/mvnw and npm EACCES during frontend dependency install.
- [x] 2026-02-26：建立本执行清单文档。
- [x] 2026-02-26：单词例句翻译链路优化完成（不使用 qwen 进行例句翻译，新增后端翻译服务与前端调用链路，补充缓存与超时策略）。
- [x] 2026-02-26：P0-1 管理端接口鉴权收口，`/api/admin/**` 增加统一 admin 角色校验（排除 `/api/admin/auth/**`）。
- [x] 2026-02-26：P0-2 关闭匿名导入，移除 `/api/vocabulary/import/**` 的匿名放行。
- [x] 2026-02-26：P0-3 导入路径白名单化，`import-from-file` 不再接受任意路径。
- [x] 2026-02-26：P0-4 生产禁用测试路由，`ApiTest/IntegrationTest/DebugTest/ShareDemo` 仅 DEV 可见。
- [x] 2026-02-26：P0-5 移除测试页面硬编码账号密码。
- [x] 2026-02-26：P0-6 CORS 收敛为配置化白名单，不再使用 `*`。
- [x] 2026-02-26：P0-7 安全配置加固：Jasypt 算法升级、移除解密口令默认回退、生产 Cookie 强制 `Secure + HttpOnly`。
- [x] 2026-02-26：P1-1 日志与监控暴露面收敛：生产环境关闭 SQL stdout，健康检查详情改为 `when_authorized`。
- [x] 2026-02-26：P1-2 前端路由预加载修正：新增递归路由预加载收集器，覆盖子路由。
- [x] 2026-02-26：P1-3 管理端请求头统一：Redis 管理页改用 `request` 封装，统一 `satoken` 头。
- [x] 2026-02-26：P1-4 自动化测试基线：新增后端鉴权测试、前端预加载单测与 `npm test` 脚本。
- [x] 2026-02-26：P2-1 拆分 `AdminController`：按域拆分为看板统计、学习内容管理、运维搜索控制器，降低单类复杂度。
- [x] 2026-02-26：P2-2 `VocabularyImporter` 日志规范化：替换 `System.out/printStackTrace` 为结构化分级日志，补充导入进度与失败上下文字段。
- [x] 2026-02-26：P2-3 文档治理完成：新增 `docs/安全相关/README.md`，并更新 `docs/开发指南/文档索引.md` 同步安全、性能、鉴权策略入口。
- [x] 2026-02-26：补充下一阶段优化建议（P3），新增异步链路、缓存策略、前端日志、路由守卫、测试覆盖与依赖升级等待办项。
- [x] 2026-02-26：P3-1 异步日志链路修复：新增独立异步写入组件 `UserLogAsyncWriter` 与 `userLogExecutor` 线程池，消除 `UserLogServiceImpl` 同类内 `@Async` 自调用风险。
- [x] 2026-02-26：P3-2 监控控制器日志规范化完成：`AdminMonitorController` 引入 `@Slf4j` 并替换 `printStackTrace` 为结构化错误日志。
- [x] 2026-02-26：P3-3 内容管理统计假数据下线：`ContentManagementServiceImpl` 改为基于 `learning_record`/`vocabulary_mastery` 的真实聚合统计，并加入热点分析缓存。
- [x] 2026-02-26：P3-4 缓存策略收敛：默认缓存管理器固定为 `cacheManager`(Caffeine L1)，Redis 缓存管理器改为显式 `redisCacheManager`，并补充命名空间与 TTL 策略。
- [x] 2026-02-26：P3-5 前端生产日志治理完成：`VocabularyView/SpeakingView/AudioPlayer` 移除直接 `console.*` 调用，统一接入 `logger`，生产仅保留 `warn/error`。
- [x] 2026-02-26：P3-6 路由守卫请求优化完成：`systemStore` 增加并发去重与后台刷新策略，`beforeEach` 调整为“首屏阻塞拉取 + 后续非阻塞刷新 + 失败降级”。
- [x] 2026-02-26：P3-7 测试覆盖扩展完成：新增后端 `AuthController` 登录限流契约测试、`AIGenerationController` 配额降级回退测试；前端新增路由守卫系统配置同步策略单测，并保留导入鉴权测试覆盖。
- [x] 2026-02-26：P3-8 依赖与基线升级计划文档完成：新增 `docs/性能优化/依赖与基线升级计划.md`，明确基线盘点、分阶段升级、验证矩阵与回滚方案。
- [x] 2026-02-26：P3-8-1 依赖基线清单与检查表完成：新增 `docs/性能优化/依赖基线清单与检查表.md`，沉淀后端/双前端/语音/CI 基线与升级前检查卡。
- [x] 2026-02-26：P3-8-2 Node 基线统一完成：CI 从 Node 18 升至 20 LTS，新增根目录 `.nvmrc`，并为 `frontend-vue/admin-vue` 增加 `engines.node` 约束。
- [x] 2026-02-26：P3-8-3 Spring Boot 第一跳升级完成：`backend/pom.xml` 从 `3.0.5` 升级到 `3.2.12`（本地因缺少 `mvn` 未执行回归，待 CI 验证）。
- [x] 2026-02-26：P3-8-4 Spring Boot 第二跳升级完成：`backend/pom.xml` 从 `3.2.12` 升级到 `3.3.0`（本地因缺少 `mvn` 未执行回归，待 CI 验证）。
- [x] 2026-02-26：P3-8-5 语音依赖固化首版完成：新增 `backend/requirements-voice.txt`，并将 `start_edge_tts.bat` 调整为优先按依赖文件安装。

## P0（高优先级）
- [x] 管理端接口鉴权收口：对 `/api/admin/**` 统一增加管理员角色校验（仅登录不等于管理员）。
- [x] 关闭公开导入面：下线或加严 `/api/vocabulary/import/**` 的匿名导入能力。
- [x] 文件路径安全：移除任意 `filePath` 入参导入，改为后端白名单路径。
- [x] 前端测试页面下线：生产环境禁用 `ApiTest/IntegrationTest/DebugTest` 路由。
- [x] 移除明文测试凭据：清理测试页面中的硬编码账号密码。
- [x] CORS 收敛：从 `*` 改为环境化白名单域名。
- [x] 安全配置加固：升级 Jasypt 算法、移除默认解密口令回退值、生产 Cookie 强制 `Secure + HttpOnly`。

## P1（中优先级）
- [x] 日志与监控暴露面收敛：生产环境关闭 debug SQL stdout，`health.show-details` 改为受限可见。
- [x] 前端路由预加载修正：修复只扫描顶层路由导致学习子路由未有效预加载的问题。
- [x] 管理端请求头统一：Redis 管理页改为统一使用封装的 `request`（`satoken` 头一致）。
- [x] 建立自动化测试基线：补充后端关键鉴权测试、前端关键链路测试与 npm/maven test 脚本。

## P2（结构性优化）
- [x] 大文件拆分：拆分 `AdminController`（当前体量过大）到按域控制器，降低耦合。
- [x] 工具类日志规范化：`VocabularyImporter` 替换 `System.out/printStackTrace` 为结构化日志。
- [x] 文档治理：将安全、性能、鉴权策略同步到 `docs/安全相关` 与 `docs/开发指南` 索引。

## P3（下一阶段优化建议）
- [x] 异步日志链路修复：`UserLogServiceImpl` 存在同类内 `@Async` 自调用（`logSuccess/logFail -> log`），实际可能未走异步代理；拆分到独立异步组件并显式线程池（如 `@Async("aiTaskExecutor")` 或新增 `userLogExecutor`）。
  - 参考：`backend/src/main/java/com/learnsphere/service/impl/UserLogServiceImpl.java`
- [x] 监控控制器日志规范化：清理 `printStackTrace`，统一为结构化日志，避免标准输出噪声并补齐错误上下文字段。
  - 参考：`backend/src/main/java/com/learnsphere/controller/admin/AdminMonitorController.java`
- [x] 内容管理统计“假数据”下线：`ContentManagementServiceImpl` 仍使用 `new Random()` 生成统计值；改为真实聚合查询 + 缓存，避免后台数据失真。
  - 参考：`backend/src/main/java/com/learnsphere/service/impl/ContentManagementServiceImpl.java`
- [x] 缓存策略收敛：当前同时存在 `Caffeine` 与 `RedisCacheManager`，且 `spring.cache.type=caffeine` + `@Primary` 可能导致 Redis 缓存能力未按预期参与；建议明确 L1/L2 分工、命名空间与 TTL。
  - 参考：`backend/src/main/resources/application.yml`、`backend/src/main/java/com/learnsphere/config/CacheConfig.java`、`backend/src/main/java/com/learnsphere/config/RedisConfig.java`
- [x] 前端生产日志治理：页面与组件中存在大量 `console.log`；统一改为 `logger` 封装，并在生产构建中去除调试日志（保留 warn/error）。
  - 参考：`frontend-vue/src/views/VocabularyView.vue`、`frontend-vue/src/views/SpeakingView.vue`、`frontend-vue/src/components/AudioPlayer.vue`
- [x] 路由守卫请求优化：`beforeEach` 中每次导航 `await fetchSystemConfig()`，虽有 TTL 仍会引入导航阻塞；改为后台刷新策略（首屏拉取 + 非阻塞更新 + 失败降级）。
  - 参考：`frontend-vue/src/router/index.js`、`frontend-vue/src/stores/system.js`
- [x] 测试覆盖扩展：当前后端仅少量鉴权测试、前端仅单测一项；补齐“登录/鉴权/导入/AI调用降级/路由守卫”关键链路回归测试。
  - 参考：`backend/src/test`、`frontend-vue/src/utils/__tests__`
- [x] 依赖与基线升级计划：后端起点为 `Spring Boot 3.0.5`，已完成两跳升级到 `3.3.0`，并形成分阶段方案（依赖清单、兼容验证、回滚方案），降低漏洞和维护成本。
  - 参考：`backend/pom.xml`、`docs/性能优化/依赖与基线升级计划.md`

## 执行节奏建议
1. 先完成全部 P0（安全边界）。
2. 再完成 P1（稳定性与可维护性）。
3. 最后推进 P2/P3（结构治理与中长期优化）。


