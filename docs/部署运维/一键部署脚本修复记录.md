# 一键部署脚本修复记录

## 更新时间
2026-03-07

## 修复内容
1. **中文乱码修复**：修复了 `deploy.bat` 在特定环境下（如系统默认 GBK 编码）打开或执行时出现的中文乱码问题。重新以 UTF-8 编码保存了所有原有的中文提示信息（如：步骤说明、清理静态目录、构建进度、打包状态以及完成提示等）。
2. **特殊字符转义**：针对批处理文件中的重定向符号（如 `>`），在打印缓存策略时使用 `^>` 进行了转义，防止执行时意外创建或覆盖文件。
3. **文档引用更新**：将最后步骤中给出的不存在的部署文档参考 `docs\DEPLOYMENT.md` 修改为了正确的实际文档路径 `docs\部署运维\宝塔部署指南.md`，以免误导使用者。

## 影响范围
- `e:\Project\LearnSphere  AI\deploy.bat`

## 验证方式
可以通过双击或在终端中运行 `deploy.bat`，终端界面应当能正常显示如下的菜单和提示信息而没有乱码：
```
========================================
 LearnSphere AI 一键部署脚本
========================================
```
