# 项目优化任务清单

## 当前执行进展

- 2026-03-11：已将 `VocabularyLearnPanel` 继续拆分为 `VocabularyLearnStartPanel` / `VocabularyLearnSessionPanel` / `VocabularyLearnResultPanel` 三个子组件，词汇学习区容器改为按状态切换编排。
- 2026-03-11：已将 `ListeningAnalysisPanel` 继续拆分为 `ListeningAnalysisHeader` / `ListeningPassageAnalysisCard` 两个子组件，解析页容器仅保留头部与篇章解析卡编排。
- 2026-03-11：已将 `ListeningPracticePanel` 继续拆分为 `ListeningAudioBlock` / `ListeningQuestionCard` 两个子组件，听力做题区容器仅保留音频块与题目卡编排。
- 2026-03-11：已将 `ListeningSetupPanel` 继续拆分为 `ListeningSetupConfigPanel` / `ListeningSetupHistorySection` 两个子组件，听力设置页容器仅保留配置区与历史区编排。
- 2026-03-11：已将 `SpeakingView.vue` 继续拆分为 `SpeakingSetupPanel` / `SpeakingPracticePanel` 两个子组件，口语页容器改为只保留阶段切换、分享状态与 AI 助手编排。
- 2026-03-11：已将 `ReadingView.vue` 继续拆分为 `ReadingSetupPanel` / `ReadingExamPanel` 两个子组件，阅读页容器改为只保留阶段切换、AI 助手与结果/复盘面板编排。
- 2026-03-11：已将 `WritingSetupPanel` 继续拆分为 `WritingSetupOptionSection` / `WritingSetupSidePanel` 两个子组件，设置页容器仅保留网格编排与历史记录挂载。
- 2026-03-10：已将 `WritingResultPanel` 继续拆分为 `WritingResultScoreCard` / `WritingResultFeedbackList` 两个子组件，结果页容器仅保留分享弹窗与展示编排。
- 2026-03-10：已将 `WritingEditorPanel` 继续拆分为 `WritingEditorToolbar` / `WritingTopicCard` / `WritingEditorCard` 三个子组件，写作编辑区改为轻量编排，后续可继续按结果区与设置区相同方式细拆。
- 2026-03-10：已抽离通用 AI 评估遮罩为 `AiEvaluatingOverlay` 组件，写作页改为复用组件，便于后续其它练习页共用。
- 2026-03-10：已抽离写作页历史列表为独立组件 `WritingHistoryPanel`，供设置页复用，进一步降低 `WritingSetupPanel` 体积。
- 2026-03-10：已将写作页拆分为 `WritingSetupPanel` / `WritingEditorPanel` / `WritingResultPanel` 三个组件，并改为异步加载，写作页仅保留容器与弹窗编排。
- 2026-03-10：已将写作页 `WritingView.vue` 的状态与业务逻辑抽离到 `frontend-vue/src/composables/useWritingPractice.js`，页面脚本改为轻量编排，为后续拆分写作配置区/编辑区/结果区组件做准备。
- 2026-03-10：已将中文乱码扫描接入 CI 自动检测（GitHub Actions），并补充排除后端数据备份/临时 SQL 目录规则，避免误报与大文件干扰。
- 2026-03-07：已移除未被业务引用的重复题库文件 `frontend-vue/src/data/tem8_words_updated.js`。
- 2026-03-07：已优化 `deploy.bat`，前后端安装优先使用 `npm ci`，并在复制管理后台构建产物前清空 `backend/src/main/resources/static/admin`，避免旧 hash 静态资源残留。
- 2026-03-07：已新增统一词库导入来源注册表，收口 `VocabularyImportController` 与 `VocabularyManagementController` 的重复硬编码路径，并修复导入管理相关注释中的中文乱码/错别字。
- 2026-03-07：已补充忽略规则，忽略 `backend/.pip-tmp/` 与 `frontend-vue/src/data/*_updated.js`，减少临时文件和中间产物回流仓库。
- 2026-03-07：已移除中间题库文件 `frontend-vue/src/data/tem4_words_fixed.js`，并补充 `*_fixed.js` 忽略规则；同时清理 `LearningHistoryController`、`AdminDashboardController`、`LearningHistoryService`、`AITutorConversationMapper` 中的历史中文乱码/错别字注释。
- 2026-03-07：已新增乱码自查脚本 `scripts/扫描中文乱码.ps1`，支持针对当前改动文件执行快速扫描，便于后续持续自检。
- 2026-03-07：已修复 `test_permissions_system.bat` 的整文件中文乱码，并删除无引用的临时对比文件 `diff.txt`；同时完善乱码扫描脚本的排除规则，避免被 `dist/target/.idea` 等非源码产物干扰。
- 2026-03-08：已新增 `scripts/同步静态资源.ps1`，将前端/后台 `dist` 到 `backend/src/main/resources/static` 的清理与复制流程脚本化；`deploy.bat` 和部署文档已改为复用该脚本，减少静态产物同步逻辑分散的问题。
- 2026-03-08：已新增 `scripts/构建前端应用.ps1`，将 `npm ci / npm install / npm run build` 收口为通用构建脚本；`deploy.bat` 和部署文档已改为复用，减少前台与后台重复构建逻辑。
- 2026-03-08：已为 `admin-vue/vite.config.js` 增加管理后台构建分包策略，将 `vue`、`echarts`、`zrender`、图标与工具依赖拆分为独立 chunk，降低管理后台单一大包压力。
- 2026-03-08：已将 `admin-vue/src/views/Users.vue` 的 `xlsx` 改为按需动态加载，并在构建分包中单独拆出 `xlsx-vendor`，避免导出 Excel 依赖进入常规访问路径。
- 2026-03-08：已新增 `admin-vue/src/utils/echarts.js`，将管理后台图表统一改为基于 `echarts/core` 的按需注册，仅保留 `line/bar/radar/funnel/pie/gauge` 等实际使用能力，继续压缩图表相关构建体积。
- 2026-03-08：已新增 `admin-vue/src/utils/gsap.js`，将 `Dashboard` 与 `AIGovernance` 中的 `gsap` 改为按需动态加载，并单独拆出 `gsap-vendor`，减少普通页面不必要的动画依赖负担。
- 2026-03-08：已完成最新一轮管理后台构建验证：`npm run build` 通过，`gsap-vendor` 成功独立输出为约 `70.32 kB`，`utils` 收敛到约 `38.65 kB`，主入口 `index` 保持约 `263.28 kB`；同时已完成中文乱码变更扫描（11 个文件）与全量扫描（704 个文件），均未发现异常。
- 2026-03-08：已将 `admin-vue/src/App.vue` 中内联的全局命令面板拆分为异步组件 `admin-vue/src/components/AppCommandPalette.vue`，仅在用户触发 `Ctrl/Cmd + K` 时加载；构建后新增按需分块 `AppCommandPalette`（JS 约 `6.44 kB`，CSS 约 `2.50 kB`），主入口 `index` 进一步由约 `263.28 kB` 降至约 `245.83 kB`。
- 2026-03-08：已将 `Content.vue` / `Grammar.vue` 中仅在编辑弹窗内使用的 `QuestionEditor` 改为异步加载，并将登录页协议弹窗 `AgreementModal` 改为打开时再加载；构建后新增 `QuestionEditor` 按需分块（JS 约 `3.49 kB`，CSS 约 `1.20 kB`）与 `AgreementModal` 按需分块（JS 约 `2.63 kB`，CSS 约 `0.30 kB`），其中 `Content` 路由脚本进一步降至约 `18.94 kB`。
- 2026-03-08：已新增 `admin-vue/src/components/AsyncDatePicker.vue`，并将 `Users.vue` 的 VIP 设置弹窗、`Notifications.vue` 的发送通知弹窗中的 `DatePicker` 改为异步加载，同时仅在弹窗打开时挂载对应表单；构建后 `DatePicker` 相关能力独立输出为 `AsyncDatePicker` chunk（JS 约 `106.62 kB`），避免普通用户列表与通知列表浏览时提前加载日期选择器依赖。
- 2026-03-08：已清理管理后台未引用的旧组件文件：删除 `CommandPalette.vue`、`DetailDrawer.vue`、`RethinkSandbox.vue`、`SkeletonLoader.vue`、`SkeletonWrapper.vue`、`Sparkline.vue`，减少无效代码与后续维护噪音；其中旧版 `CommandPalette.vue` 已由新的 `AppCommandPalette.vue` + `CommandPaletteLoader.js` 方案替代。
- 2026-03-08：已将 `admin-vue/src/views/Users.vue` 中的 360 用户详情弹窗抽离为异步组件 `admin-vue/src/components/UserDetailModal.vue`，把详情数据拉取、AI 日志和画像图表初始化改为仅在打开弹窗时加载；最新构建中新增 `UserDetailModal` 按需分块（JS 约 `27.87 kB`，CSS 约 `0.92 kB`），`Users` 路由脚本保持约 `22.14 kB`，对应路由样式降至约 `0.75 kB`。
- 2026-03-08：已继续将 `admin-vue/src/views/Users.vue` 中的 `VIP 设置`、`编辑资料`、`重置密码`、`批量赠送 VIP`、`批量发送通知`、`高级筛选` 六个低频弹窗拆为异步组件，进一步收口用户页批量操作与弹窗逻辑；最新构建中 `Users` 路由脚本进一步降至约 `11.53 kB`，对应重弹窗能力改为按需加载。
- 2026-03-08：已将 `admin-vue/src/views/AIGovernance.vue` 中的提示词编辑、版本历史、AI 日志审计三个低频重弹窗拆为异步组件 `admin-vue/src/components/AIGovernancePromptEditorModal.vue`、`admin-vue/src/components/AIGovernancePromptHistoryModal.vue`、`admin-vue/src/components/AIGovernanceLogDetailModal.vue`；构建后 `AIGovernance` 路由脚本由约 `53.56 kB` 降至约 `47.08 kB`，路由样式由约 `4.89 kB` 降至约 `3.86 kB`，并新增对应按需分块（JS 约 `4.06 / 4.23 / 4.18 kB`，CSS 约 `0.19 / 0.81 kB`）。
- 2026-03-08：已继续将 `admin-vue/src/views/AIGovernance.vue` 中的 A/B 实验创建弹窗与实验报告弹窗拆为异步组件 `admin-vue/src/components/AIGovernanceExperimentModal.vue`、`admin-vue/src/components/AIGovernanceExperimentReportModal.vue`；构建后 `AIGovernance` 路由脚本进一步由约 `47.08 kB` 降至约 `44.09 kB`，并新增对应按需分块（JS 约 `3.05 / 3.53 kB`）。
- 2026-03-08：已继续将 `admin-vue/src/views/AIGovernance.vue` 的 `提示词工程`、`运行日志`、`A/B 实验室` 三个非默认 Tab 抽离为异步组件 `admin-vue/src/components/AIGovernancePromptTab.vue`、`admin-vue/src/components/AIGovernanceLogsTab.vue`、`admin-vue/src/components/AIGovernanceExperimentTab.vue`；构建后 `AIGovernance` 路由脚本进一步由约 `44.09 kB` 降至约 `36.62 kB`，并新增对应按需分块（JS 约 `4.87 / 4.39 / 4.07 kB`）。
- 2026-03-08：已继续将 `admin-vue/src/views/AIGovernance.vue` 的 `反馈闭环与自进化`、`Prompt 沙箱` 两个 Tab 抽离为异步组件 `admin-vue/src/components/AIGovernanceLoopTab.vue`、`admin-vue/src/components/AIGovernanceSandboxTab.vue`，并移除治理页首屏对 `AI 模型目录` / `AI 配置` 的预加载；构建后 `AIGovernance` 路由脚本进一步由约 `36.62 kB` 降至约 `28.89 kB`，新增对应按需分块（JS 约 `7.53 / 2.82 kB`）。
- 2026-03-08：已继续将 `admin-vue/src/views/AIGovernance.vue` 的 `稳定性与工程` Tab 抽离为异步组件 `admin-vue/src/components/AIGovernanceStabilityTab.vue`，并将 `AI 健康状态`、`AI 模型目录`、`AI 配置` 加载下沉到子组件；构建后 `AIGovernance` 路由脚本进一步由约 `28.89 kB` 降至约 `17.94 kB`，新增对应按需分块（JS 约 `12.76 kB`，CSS 约 `0.96 kB`），父页面代码收敛到约 `894` 行。
- 2026-03-08：已继续将 `admin-vue/src/views/AIGovernance.vue` 的默认 `全景监控` Tab 抽离为异步组件 `admin-vue/src/components/AIGovernanceMonitorTab.vue`，并把监控数据拉取、图表渲染、入场动画、刷新暴露统一下沉到子组件；构建后 `AIGovernance` 路由脚本进一步由约 `17.94 kB` 降至约 `4.99 kB`，路由样式由约 `3.86 kB` 进一步降至约 `0.75 kB`，新增对应按需分块（JS 约 `15.24 kB`，CSS 约 `1.67 kB`），父页面代码收敛到约 `138` 行。
- 2026-03-08：本轮已继续推进后台治理页拆分与构建验证；下一优先项建议转入前台学习页 `GrammarView.vue`、`ListeningView.vue`、`VocabularyView.vue` 的容器化拆分。
- 2026-03-08：已开始转入前台学习页拆分：将 `frontend-vue/src/views/GrammarView.vue` 的状态、接口调用与练习流程抽离到 `frontend-vue/src/composables/useGrammarPractice.js`，并新增 `frontend-vue/src/constants/grammarPractice.js` 收口知识点/模式/难度常量；构建验证通过，`GrammarView.vue` 文件已收敛到约 `1411` 行，主状态逻辑迁移为约 `556` 行 composable，当前路由构建产物约 `29.91 kB`。
- 2026-03-08：本轮已继续推进后台治理页与前台语法页拆分；下一优先项建议继续把 `GrammarView.vue` 的题目区、结果区、侧边栏拆为展示组件，并复用到 `ListeningView.vue`、`VocabularyView.vue`。
- 2026-03-08：已继续将 `frontend-vue/src/views/GrammarView.vue` 的右侧学习状态/练习配置/当前任务区抽离为展示组件 `frontend-vue/src/components/grammar/GrammarSidebarPanel.vue`；父页面进一步收敛到约 `1307` 行，侧边栏展示逻辑独立成约 `474` 行组件，构建验证继续通过。
- 2026-03-08：已继续将 `frontend-vue/src/views/GrammarView.vue` 的题目区与结果区拆为展示组件 `frontend-vue/src/components/grammar/GrammarQuestionPanel.vue`、`frontend-vue/src/components/grammar/GrammarResultPanel.vue`，并改为异步加载；父页面进一步收敛到约 `1165` 行，最新构建中 `GrammarView` 路由脚本降至约 `24.90 kB`，并新增对应按需分块（JS 约 `6.25 / 4.57 kB`，CSS 约 `4.23 / 2.17 kB`）。
- 2026-03-08：已开始将 `frontend-vue/src/views/ListeningView.vue` 的测试态播放区/题目区与结果区拆为异步组件 `frontend-vue/src/components/listening/ListeningPracticePanel.vue`、`frontend-vue/src/components/listening/ListeningResultPanel.vue`；父页面先收敛到约 `1677` 行，最新构建中 `ListeningView` 路由脚本约 `27.42 kB`，并新增对应按需分块（JS 约 `3.91 / 1.88 kB`，CSS 约 `3.05 / 0.47 kB`）。
- 2026-03-09：已继续将 `frontend-vue/src/views/ListeningView.vue` 的设置态与详细解析区拆为异步组件 `frontend-vue/src/components/listening/ListeningSetupPanel.vue`、`frontend-vue/src/components/listening/ListeningAnalysisPanel.vue`；父页面进一步收敛到约 `1180` 行，最新构建中 `ListeningView` 路由脚本进一步降至约 `21.09 kB`，并新增对应按需分块（JS 约 `6.07 / 3.57 kB`，CSS 约 `3.52 / 2.13 kB`）。
- 2026-03-09：已继续拆分 `frontend-vue/src/views/VocabularyView.vue`：先将浏览区、学习区、详情弹窗拆为异步组件 `frontend-vue/src/components/vocabulary/VocabularyBrowsePanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyLearnPanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyDetailModal.vue`，再将顶部日任务与统计总览抽离为 `frontend-vue/src/components/vocabulary/VocabularyOverviewHeader.vue`；父页面已由约 `1402` 行收敛到约 `674` 行，最新构建中 `VocabularyView` 路由脚本进一步收敛到约 `16.91 kB`、路由样式收敛到约 `0.47 kB`，并新增对应按需分块（JS 约 `3.49 / 3.32 / 6.54 / 2.62 kB`，CSS 约 `2.58 / 1.83 / 7.00 / 1.91 kB`）。
- 2026-03-09：已继续将 `frontend-vue/src/views/VocabularyView.vue` 的业务脚本下沉到 `frontend-vue/src/composables/useVocabularyPractice.js`，把音频播放、浏览数据加载、详情补例句、学习会话、AI 助记与 AI Tutor 上下文统一收口；父页面进一步由约 `674` 行收敛到约 `184` 行，同时修正浏览页切换考试类型时的重复列表请求。最新构建验证通过，`VocabularyView` 路由脚本约 `18.55 kB`、路由样式约 `0.47 kB`。
- 2026-03-09：已继续把词汇页私有音频逻辑抽离为通用 composable `frontend-vue/src/composables/useTextAudio.js`，并回接到 `frontend-vue/src/composables/useVocabularyPractice.js`，在保持现有在线音源 + 原生 TTS 降级行为不变的前提下，为后续 `ListeningView` / `SpeakingMockView` 复用做准备；`useVocabularyPractice.js` 已进一步收敛到约 `389` 行。
- 2026-03-09：已继续将 `frontend-vue/src/views/SpeakingMockView.vue` 的 AI 考官播报改为复用 `frontend-vue/src/composables/useTextAudio.js`，统一语音预热、停止与原生 TTS 选择逻辑，同时保留该页面的自动播报开关与音量控制；构建验证继续通过。
- 2026-03-09：已继续将 `frontend-vue/src/views/ListeningView.vue` 的原生 TTS fallback 与停止逻辑接入 `frontend-vue/src/composables/useTextAudio.js`，保留本地 Edge TTS 缓存、预加载和进度显示逻辑不变，仅把原生播报收口到公共层；同时 `useTextAudio.js` 增强为支持每次播放传入选音、音量与事件回调。最新构建中 `ListeningView` 路由脚本约 `20.78 kB`，`SpeakingMockView` 路由脚本约 `31.32 kB`。
- 2026-03-09：已继续将 `frontend-vue/src/views/MockExamView.vue`、`frontend-vue/src/components/FlashCard.vue` 的原生朗读切换到 `frontend-vue/src/composables/useTextAudio.js`，并同步移除已无业务引用的旧工具 `frontend-vue/src/utils/tts.js`；`frontend-vue/src/views/SettingsView.vue` 的缓存清理改为仅停止浏览器原生播报，不再依赖历史 TTS 单例。最新前台构建验证通过，`MockExamView` 路由脚本约 `21.32 kB`。
- 2026-03-09：已继续将 `frontend-vue/src/components/AudioPlayer.vue` 的本地 TTS fallback 接入 `frontend-vue/src/composables/useTextAudio.js`，保留组件内远程音频播放、进度估算、书签与笔记逻辑，仅把原生播报的启动、停止与错误处理统一到公共音频层；同时 `frontend-vue/src/views/VocabularyViewNew.vue` 的单词发音也已切换为复用 `useTextAudio.js`，删除页面内直连 `speechSynthesis`。最新前台构建验证继续通过，`LearningHubView` 路由脚本约 `41.81 kB`，`ListeningView` 路由脚本约 `20.78 kB`。
- 2026-03-09：已继续拆分 `frontend-vue/src/views/SpeakingView.vue`：先将口语页状态与业务流程下沉到 `frontend-vue/src/composables/useSpeakingPractice.js`，再把练习历史与评估结果区拆为异步组件 `frontend-vue/src/components/speaking/SpeakingHistoryPanel.vue`、`frontend-vue/src/components/speaking/SpeakingResultPanel.vue`；父页面已由约 `1103` 行收敛到约 `383` 行，结构基本贴近“容器 + 练习区 + 结果区 + AI 导师入口”，最新构建中 `SpeakingView` 路由脚本约 `22.21 kB`。
- 2026-03-09：已继续拆分 `frontend-vue/src/views/ReadingView.vue`：先将阅读流程、历史加载、打字机播放、答题进度、分享与 AI 导师上下文下沉到 `frontend-vue/src/composables/useReadingPractice.js`，再把最近生成历史与结果页拆为异步组件 `frontend-vue/src/components/reading/ReadingHistoryPanel.vue`、`frontend-vue/src/components/reading/ReadingResultPanel.vue`；父页面已由约 `1227` 行收敛到约 `826` 行，最新构建中 `ReadingView` 路由脚本约 `19.70 kB`。
## 一、扫描结论

整体来看，这个项目已经具备比较好的产品雏形，技术栈也不落后：前端采用 `Vue 3 + Vite`，后端采用 `Spring Boot 3`，并且已经接入了 `PWA`、`Actuator`、`Prometheus`、`Resilience4j`、限流与权限控制，说明项目不是“从零搭的 demo”，而是已经进入了“要开始做工程化收口”的阶段。

这次结合仓库现状看，后续最值得投入的优化方向，不是继续堆新功能，而是优先做下面四类事情：

1. **瘦身资源与收敛构建产物**：前端本地题库和静态资源偏重，发布物也直接进入后端源码目录，长期会拖慢构建、提交和部署。
2. **拆分超大模块**：后端 AI 生成服务、前台学习页、后台治理页都出现了“大文件承载太多职责”的迹象。
3. **补齐质量门禁**：测试覆盖、Lint、格式化和 CI 校验还不够完整，容易在后期迭代中引入回归问题。
4. **标准化部署与数据变更**：数据库初始化仍偏“启动即修库”的方式，适合快速开发，但不适合多人协作和生产演进。

## 二、当前最明显的问题点

- `frontend-vue/src/data` 下共有 **47 个数据文件**，总体积约 **14.41 MB**，本地题库资源偏重。
- `frontend-vue/src/data/tem8_words.js` 与 `frontend-vue/src/data/tem8_words_updated.js` 内容完全一致，存在可清理的重复数据。
- `backend/src/main/resources/static` 下已有前台/后台构建产物，共 **291 个静态文件**，总体积约 **7.59 MB**，说明构建结果已经直接进入后端源码目录。
- `backend/src/main/java/com/learnsphere/service/impl/AIGenerationServiceImpl.java` 约 **2109 行**，AI 业务逻辑已明显过重。
- `backend/src/main/java/com/learnsphere/config/DatabaseInitializer.java` 约 **639 行**，承担了大量“建表 + 补字段 + 修数据”的职责。
- 前台页面 `frontend-vue/src/views/GrammarView.vue` 约 **1713 行**、`frontend-vue/src/views/ListeningView.vue` 约 **1690 行**、`frontend-vue/src/views/VocabularyView.vue` 约 **1454 行**，页面逻辑偏集中。
- 后台页面 `admin-vue/src/views/AIGovernance.vue` 约 **2031 行**，后续维护成本会越来越高。
- 测试数量不均衡：后端业务测试 **4 个**，前台业务测试 **2 个**，后台业务测试 **0 个**。
- `.github/workflows/ci.yml` 已存在，但目前前端/后台主要执行构建，质量门禁还不够完整。
- 仓库中暂未发现统一的 `ESLint / Prettier / Checkstyle / Spotless` 配置，代码规范更多依赖人工自觉。
- `frontend-vue` 与 `admin-vue` 的 `vue`、`vite`、`axios` 版本并不完全一致，后续维护会有额外心智成本。
- 仓库里暂未看到正式的 `Dockerfile` / `docker-compose`，部署标准化仍有提升空间。

## 三、建议先创建的优化任务

下面按优先级整理为可以直接推进的任务清单。

### P0：建议优先处理

- [ ] **任务 1：收敛前端题库与静态资源体积**
  - 目标：降低首包压力、减少构建时间、减轻仓库体积增长。
  - 建议动作：
    - 将 `frontend-vue/src/data` 的题库数据按考试类型、难度、模块拆分为可按需加载的资源。
    - 重复数据先清理，至少先处理 `tem8_words.js` 与 `tem8_words_updated.js` 这类重复文件。
    - 中长期建议把大题库迁移到后端接口、对象存储或 CDN，而不是长期放在前端源码中。
  - 预期收益：用户首次打开更快，前端构建更轻，内容迭代也更方便。

- [ ] **任务 2：停止将构建产物长期保留在源码目录中**
  - 目标：减少脏提交、避免旧资源残留、让发布流程更清晰。
  - 建议动作：
    - 明确 `frontend-vue`、`admin-vue` 的构建产物由 CI/CD 生成，再复制到后端发布目录。
    - 清理 `backend/src/main/resources/static` 内历史构建残留，避免多次 build 后旧 hash 文件持续堆积。
    - 将“源码”和“构建输出”职责彻底分开。
  - 预期收益：Git 历史更干净，部署包更可控，线上回滚也更清楚。

- [ ] **任务 3：拆分 AI 核心服务，降低单类复杂度**
  - 目标：提升 AI 相关代码可读性、可测试性和可扩展性。
  - 建议动作：
    - 将 `AIGenerationServiceImpl.java` 拆为至少几类职责：提示词组装、模型路由、配额扣减、降级兜底、日志记录、指标埋点。
    - Controller 保持“薄”，复杂编排尽量沉到专门的 orchestrator/service 中。
    - 与 AI 相关的 DTO、策略、模板、限流和审计规则，尽量分包管理。
  - 预期收益：后面新增模型、切换供应商、增加配额策略时，改动范围更小。

- [ ] **任务 4：把数据库变更改为正式迁移机制**
  - 目标：让数据库演进可追踪、可回滚、可多环境复现。
  - 建议动作：
    - 逐步用 `Flyway` 或 `Liquibase` 取代 `DatabaseInitializer.java` 中的大量启动时 SQL。
    - 将根目录零散的 SQL 脚本统一收口到后端 migration 目录管理。
    - 把“初始化建库”和“版本迁移”区分开，不再依赖应用启动时自动修表。
  - 预期收益：避免线上环境启动即改库带来的不确定性，也更适合团队协作。

### P1：建议在 P0 后紧接着做

- [ ] **任务 5：补齐测试、Lint 与格式化门禁**
  - 目标：把“能跑”提升为“可持续迭代”。
  - 建议动作：
    - 前台和后台都补 `ESLint + Prettier`。
    - 后端补 `Spotless` 或 `Checkstyle`，至少形成基础统一规范。
    - CI 中除了构建，还应执行前端测试、后台测试、后端测试与必要的静态检查。
    - 优先补高风险模块测试：登录鉴权、AI 生成、学习记录、管理后台治理配置。
  - 预期收益：后续改动更有安全感，减少“修一个地方坏一片”的情况。

- [ ] **任务 6：拆分前台大页面与后台治理页**
  - 目标：降低页面耦合度，提升功能扩展效率。
  - 当前已完成：
    - `admin-vue/src/App.vue` 中的全局命令面板已拆为异步组件 `admin-vue/src/components/AppCommandPalette.vue`，仅在触发快捷键时加载。
    - `admin-vue/src/views/Content.vue`、`admin-vue/src/views/Grammar.vue` 中仅编辑弹窗使用的 `QuestionEditor` 已改为按需加载。
    - `admin-vue/src/views/Users.vue` 中的 360 用户详情弹窗已抽离为异步组件 `admin-vue/src/components/UserDetailModal.vue`，详情数据、AI 日志和画像图表改为打开时再加载。
    - `admin-vue/src/views/Users.vue` 中的 VIP 设置、编辑资料、重置密码、批量赠送 VIP、批量发送通知、高级筛选弹窗已进一步拆为独立异步组件，列表页只保留打开入口与结果回写逻辑。
    - `admin-vue/src/views/AIGovernance.vue` 中的提示词编辑、版本历史、AI 日志审计三个低频重弹窗已拆为异步组件，避免治理页常规浏览时提前加载完整弹窗内容。
    - `admin-vue/src/views/AIGovernance.vue` 中的 A/B 实验创建弹窗与实验报告弹窗也已拆为异步组件，治理页的实验能力改为按需进入。
    - `admin-vue/src/views/AIGovernance.vue` 中的提示词工程、运行日志、A/B 实验室三个非默认 Tab 已进一步拆为独立异步组件，只有切入对应治理场景时才加载表格、过滤器和实验操作逻辑。
    - `admin-vue/src/views/AIGovernance.vue` 中的反馈闭环与自进化、Prompt 沙箱两个 Tab 也已继续拆为独立异步组件，并延后 AI 模型目录与 AI 配置加载时机，减少治理页默认进入路径的额外状态与请求负担。
    - `admin-vue/src/views/AIGovernance.vue` 中的稳定性与工程 Tab 已继续拆为独立异步组件 `admin-vue/src/components/AIGovernanceStabilityTab.vue`，治理页父组件仅保留页签编排，模型切换与健康检查逻辑改为进入对应页签时再加载。
    - `admin-vue/src/views/AIGovernance.vue` 中的默认全景监控 Tab 已继续拆为独立异步组件 `admin-vue/src/components/AIGovernanceMonitorTab.vue`，监控数据拉取、图表渲染、入场动画和刷新能力统一收口到子组件，治理页父组件进一步缩减为页签容器。
    - `frontend-vue/src/views/GrammarView.vue` 已先完成状态层抽离，练习流程、历史记录、统计拉取、AI 学习建议与离开保护已收口到 `frontend-vue/src/composables/useGrammarPractice.js`，页面主文件改为以模板与展示绑定为主。
    - `frontend-vue/src/views/GrammarView.vue` 的右侧学习状态、练习配置与题目导航已继续拆为展示组件 `frontend-vue/src/components/grammar/GrammarSidebarPanel.vue`，父页面继续向“容器 + 绑定”收口。
    - `frontend-vue/src/views/GrammarView.vue` 的题目区与结果区也已继续拆为展示组件 `frontend-vue/src/components/grammar/GrammarQuestionPanel.vue`、`frontend-vue/src/components/grammar/GrammarResultPanel.vue`，并改为异步加载，父页面仅保留启动态、容器编排与事件透传。
    - `frontend-vue/src/views/ListeningView.vue` 已将测试态播放区、题目区、结果区、设置态与详细解析区分别拆为异步组件 `frontend-vue/src/components/listening/ListeningPracticePanel.vue`、`frontend-vue/src/components/listening/ListeningResultPanel.vue`、`frontend-vue/src/components/listening/ListeningSetupPanel.vue`、`frontend-vue/src/components/listening/ListeningAnalysisPanel.vue`，父页面进一步向“容器 + 状态编排 + 侧栏导航”收口。
    - `frontend-vue/src/views/VocabularyView.vue` 已将浏览区、学习区、详情弹窗与顶部总览区继续拆为异步组件 `frontend-vue/src/components/vocabulary/VocabularyBrowsePanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyLearnPanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyDetailModal.vue`、`frontend-vue/src/components/vocabulary/VocabularyOverviewHeader.vue`，并进一步把业务脚本下沉到 `frontend-vue/src/composables/useVocabularyPractice.js`；当前父页面已收敛到约 `184` 行，结构基本贴近“容器 + 绑定”。
    - `frontend-vue/src/views/SpeakingMockView.vue`、`frontend-vue/src/views/ListeningView.vue`、`frontend-vue/src/views/MockExamView.vue`、`frontend-vue/src/components/FlashCard.vue`、`frontend-vue/src/components/AudioPlayer.vue`、`frontend-vue/src/views/VocabularyViewNew.vue` 已将页面内原生朗读或本地 TTS fallback 统一到 `frontend-vue/src/composables/useTextAudio.js`；`frontend-vue/src/views/SettingsView.vue` 仅保留缓存清理时的浏览器播报停止逻辑。
    - `frontend-vue/src/views/SpeakingView.vue` 已将练习状态、录音识别、历史加载与评估提交下沉到 `frontend-vue/src/composables/useSpeakingPractice.js`，并将结果区、历史区拆为异步组件 `frontend-vue/src/components/speaking/SpeakingResultPanel.vue`、`frontend-vue/src/components/speaking/SpeakingHistoryPanel.vue`；当前父页面已收敛到约 `383` 行。
    - `frontend-vue/src/views/ReadingView.vue` 已将阅读流程、历史加载、打字机播放、答题进度、分享与 AI 导师上下文下沉到 `frontend-vue/src/composables/useReadingPractice.js`，并将最近生成历史区、结果区、解析区拆为异步组件 `frontend-vue/src/components/reading/ReadingHistoryPanel.vue`、`frontend-vue/src/components/reading/ReadingResultPanel.vue`、`frontend-vue/src/components/reading/ReadingReviewPanel.vue`；当前父页面已进一步收敛到约 `775` 行，构建产物中额外拆出 `ReadingReviewPanel` 约 `0.39 kB`，`ReadingView` 路由脚本约 `18.04 kB`。
    - `frontend-vue/src/views/ProfileView.vue` 已将资料同步、头像上传、MFA 绑定、安全评分、配额轮询与成就加载下沉到 `frontend-vue/src/composables/useProfileSettings.js`，并将会员权益区、成就区、安全设置区与 MFA 弹窗继续拆为异步组件 `frontend-vue/src/components/profile/ProfileVipPanel.vue`、`frontend-vue/src/components/profile/ProfileAchievementPanel.vue`、`frontend-vue/src/components/profile/ProfileSecurityPanel.vue`、`frontend-vue/src/components/profile/ProfileMfaModal.vue`；当前父页面已进一步收敛到约 `209` 行，构建产物中额外拆出 `ProfileVipPanel` 约 `10.01 kB`、`ProfileAchievementPanel` 约 `2.10 kB`、`ProfileSecurityPanel` 约 `0.69 kB`，`ProfileView` 路由脚本约 `65.89 kB`。
    - `frontend-vue/src/views/DashboardView.vue` 已将首页数据加载、签到、排行、AI 推荐、图表初始化与动画编排下沉到 `frontend-vue/src/composables/useDashboardOverview.js`，并将 AI 洞察卡、右侧榜单与最近活动区拆为异步组件 `frontend-vue/src/components/dashboard/DashboardAiInsightPanel.vue`、`frontend-vue/src/components/dashboard/DashboardSidebarPanel.vue`；当前父页面已收敛到约 `408` 行，构建产物中额外拆出 `DashboardAiInsightPanel` 约 `3.21 kB`、`DashboardSidebarPanel` 约 `2.64 kB`，`DashboardView` 路由脚本约 `15.79 kB`。
    - `admin-vue/src/views/Dashboard.vue` 已将统计数据编排、图表初始化、入场动画、AI 简报、时钟刷新与刷新逻辑下沉到 `admin-vue/src/composables/useAdminDashboard.js`，并将 KPI 区、右侧导航/留存/审计区、AI 简报弹窗拆为组件 `admin-vue/src/components/AdminDashboardKpiGrid.vue`、`admin-vue/src/components/AdminDashboardSidebarPanel.vue`、`admin-vue/src/components/AdminDashboardBriefingModal.vue`；当前父页面已由约 `1019` 行收敛到约 `344` 行，本轮后台构建已通过，构建产物中额外拆出 `AdminDashboardBriefingModal` 约 `1.95 kB`（CSS 约 `0.26 kB`），`Dashboard` 路由脚本约 `18.57 kB`。
    - `frontend-vue/src/views/VocabularyTestView.vue` 已将词汇测试配置、题目生成、判分归档、AI 导师上下文、错题/正题记录保存与庆祝动画下沉到 `frontend-vue/src/composables/useVocabularyTest.js`，并将设置页、测试页、结果页拆为异步组件 `frontend-vue/src/components/vocabulary/VocabularyTestSetupPanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyTestQuestionPanel.vue`、`frontend-vue/src/components/vocabulary/VocabularyTestResultPanel.vue`；当前父页面已由约 `889` 行收敛到约 `99` 行，构建产物中额外拆出 `VocabularyTestSetupPanel` 约 `3.58 kB`、`VocabularyTestQuestionPanel` 约 `4.14 kB`、`VocabularyTestResultPanel` 约 `2.94 kB`（对应 CSS 分别约 `2.65 kB`、`3.13 kB`、`0.42 kB`），`VocabularyTestView` 路由脚本约 `11.18 kB`，并顺手修复了结果回顾区使用过滤后索引导致答案映射错位的问题。
  - 当前进行中：
    - 前台词汇测试页与后台首页已完成一轮收口，当前优先项转为继续盘点 `frontend-vue/src/views/WritingView.vue` 与 `admin-vue/src/views/Users.vue` 等剩余重页面，决定下一批容器化拆分目标。
  - 下一步建议：
    - 优先继续拆 `frontend-vue/src/views/WritingView.vue`，把写作配置区、答题编辑区、结果区与历史区继续拆成展示组件与状态层。
    - 后台侧继续评估 `admin-vue/src/views/Users.vue` 的筛选区、批量操作区与表格工具栏，判断是否进一步容器化。
  - 预期收益：单文件不再过大，联调和排障效率会显著提高。
- [ ] **任务 7：统一前端工程管理方式**
  - 目标：减少版本漂移和重复维护成本。
  - 当前现状：
    - 根目录、`frontend-vue`、`admin-vue` 目前分别维护各自的 `package.json`，其中前后台各自保留独立 `package-lock.json`，尚未收口为 workspace。
    - 公共依赖已经部分自然收敛，例如 `gsap`、`lucide-vue-next`、`naive-ui`、`pinia`、`vue-router` 版本基本一致。
    - 仍存在明显版本漂移，例如 `axios`（根目录/前台 `^1.13.2`，后台 `^1.6.0`）、`vue`（前台 `^3.5.24`，后台 `^3.5.12`）、`vite`（前台 `^7.2.4`，后台 `^7.3.1`）。
  - 当前进行中：
    - 目前已在脚本层面对前后台构建和静态资源同步做了一定收口，但 Node 工程本身的依赖安装、锁文件和共享模块边界仍未统一。
  - 下一步建议：
    - 把根目录、`frontend-vue`、`admin-vue` 的 Node 侧依赖统一规划，优先评估使用 `npm workspaces`。
    - 先收敛 `vue`、`vite`、`axios` 等低风险公共依赖版本；像 `echarts` 这类存在大版本差异的依赖先做兼容评估，再决定是否统一。
    - 抽公共请求封装、通用组件、鉴权逻辑和常量定义，避免双端重复维护。
  - 预期收益：升级依赖时更顺手，也更适合后面继续扩展客户端。
- [ ] **任务 8：标准化部署与环境管理**
  - 目标：降低部署门槛，减少环境差异问题。
  - 当前现状：
    - 已有 `deploy.bat`、`scripts/构建前端应用.ps1`、`scripts/同步静态资源.ps1` 等脚本，前后端构建与静态资源同步流程已经比之前更集中。
    - 前后台目前存在真实环境文件 `frontend-vue/.env.development`、`frontend-vue/.env.production`、`admin-vue/.env.development`、`admin-vue/.env.production`，但仓库中还没有对应的 `.env.example` 模板。
    - 后端已区分 `application.yml`、`application-dev.yml`、`application-prod.yml`、`application-debug.yml`，并保留 `application-secret.properties`、`application-redis.properties`，但配置来源仍偏分散，新环境接手时仍需要人工辨认。
    - 仓库中暂未看到正式的 `Dockerfile`、`docker-compose.yml` 或等效容器化部署方案。
  - 当前进行中：
    - 部署流程脚本化已经推进了一步，但“环境变量模板、后端敏感配置注入方式、容器化交付物”这三块还没有形成统一标准。
  - 下一步建议：
    - 补齐正式的 `Dockerfile` / `docker-compose` 或等效部署方案，至少先覆盖单机部署和本地复现两类场景。
    - 前台、后台分别补 `.env.example`，并注明哪些变量允许提交模板、哪些变量必须通过部署环境注入。
    - 区分开发、测试、生产环境的配置来源和注入方式，尽量把密钥、数据库、Redis、邮件等敏感配置从源码内默认值中剥离出来。
  - 预期收益：新环境落地更快，迁移服务器或交接项目更省心。
### P2：适合在系统稳定后持续推进

- [ ] **任务 9：完善监控、告警与 AI 成本看板**
  - 目标：把已有监控基础真正用起来。
  - 当前现状：
    - 后端已经接入 `Actuator + Prometheus`，`application.yml` 中已有 `health / info / metrics / prometheus` 端点暴露配置，可观测性基础已经具备。
    - 仓库中已有 `docs/性能优化/观测链路实施.md`、`docs/后端架构/后端架构优化.md` 等记录，说明观测链路和指标暴露已经做过基础建设。
    - 管理后台已有 `AI 治理` 相关页面和令牌统计能力，但更偏业务台账，尚未形成 Grafana 看板、统一告警阈值和 AI 成本预算视角。
    - 目前还没有看到正式的 Grafana Dashboard 配置、Prometheus 告警规则文件，前端侧也未形成稳定的 Web Vitals 采样机制。
  - 当前进行中：
    - 现阶段已经完成“指标可暴露”的地基建设，但“指标可消费、可告警、可回溯”的闭环还没补齐。
  - 下一步建议：
    - 基于现有 `Actuator + Prometheus` 补齐 Grafana 看板，优先覆盖 JVM、接口耗时、错误率、数据库连接、Redis、AI 请求量。
    - 对 AI 请求耗时、失败率、配额消耗、降级触发次数、热点接口设置阈值告警，并补一版 Prometheus 告警规则示例。
    - 前端增加 Web Vitals 或关键路径性能采样，把首屏、路由切换和高频交互指标纳入观测面。
  - 预期收益：问题能更早暴露，AI 成本也更容易被量化管理。
- [ ] **任务 10：补强项目文档与架构说明**
  - 目标：提升项目可接手性与协作效率。
  - 当前现状：
    - 仓库里已经有大量文档，覆盖开发指南、部署运维、后端架构、AI 功能、业务功能、管理后台等多个方向，内容并不缺。
    - 现有问题主要不是“没有文档”，而是文档分散在多个目录中，主题交叉较多，缺少一套固定入口去回答“系统怎么组成、怎么部署、怎么演进、谁负责什么配置”。
    - 接口文档、后端架构说明、AI 治理说明、学习记录相关说明已经存在，但数据库迁移策略、资源归属边界、统一模块图/部署图仍不够集中。
  - 当前进行中：
    - 当前执行清单、部署文档和性能优化记录已经在持续补充，但还没有收敛成一套面向接手者的“项目总览 + 模块手册”文档体系。
  - 下一步建议：
    - 把“系统模块图、部署图、数据库迁移策略、前后端接口约定”整理成固定文档，并放到清晰的一层入口下。
    - 把“哪些资源由前端内置、哪些资源由后端提供、哪些资源可通过管理后台维护”写清楚，减少边界模糊导致的重复维护。
    - 为关键模块增加维护说明，例如 AI 治理、学习记录、权限控制、题库导入，并明确相关代码入口、配置入口和依赖关系。
  - 预期收益：后续无论是自己回头看，还是交给别人接手，成本都会低很多。
## 四、推荐执行顺序

结合当前已经完成的资源治理、构建收口和部分大页面异步拆分，我建议后续按下面顺序继续推进：

1. **先继续拆分管理后台重页面与 `DataTable` 相关区域**
2. **再补测试、Lint、CI 门禁**
3. **然后统一前端工程管理方式与公共依赖版本**
4. **接着推进数据库迁移标准化与部署/环境标准化**
5. **最后补监控告警看板与项目总览文档体系**

这样安排的好处是：

- 第一阶段延续当前收益最高的方向，继续降低 `AIGovernance`、重弹窗、重表格页面的复杂度；
- 第二阶段尽快把质量门槛立起来，避免后面越拆越容易引入回归问题；
- 第三阶段在结构趋稳后统一工程与依赖，减少版本漂移带来的维护成本；
- 第四阶段把数据演进、部署和环境注入方式沉淀成长期稳定机制；
- 第五阶段再把观测与文档体系补齐，方便长期运维、交接和持续扩展。
## 五、一句话建议

这个项目现在最需要的不是“再加更多功能”，而是把已经做出来的功能**工程化、模块化、标准化**。只要把资源管理、AI 核心服务、数据库迁移、测试门禁这四块补起来，项目的可维护性会明显上一个台阶。

