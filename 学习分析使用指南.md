# 📖 学习分析模块使用指南

## 快速开始

### 1. Advanced Analytics (高级学习分析)

#### 基础使用
```javascript
// 创建实例（自动初始化）
const analytics = new AdvancedAnalytics();

// 获取统计信息
const stats = analytics.getStats();
console.log('分析统计:', stats);

// 清理资源
analytics.cleanup();
```

#### 查看缓存效果
```javascript
// 第一次调用（无缓存）
analytics.someAnalysisMethod(); // 较慢

// 第二次调用（命中缓存）
analytics.someAnalysisMethod(); // 非常快！
```

---

### 2. Weakness Analyzer (薄弱点分析器)

#### 基础使用
```javascript
// 创建实例
const analyzer = new WeaknessAnalyzer();

// 分析学习者薄弱点
const learnerData = {
    vocabulary: { /* 词汇数据 */ },
    grammar: { /* 语法数据 */ },
    listening: { /* 听力数据 */ },
    reading: { /* 阅读数据 */ }
};

const analysis = await analyzer.analyzeWeaknesses(learnerData);

console.log('薄弱点分析结果:', analysis);
// {
//   overall: { /* 全局分析 */ },
//   vocabulary: { /* 词汇薄弱点 */ },
//   grammar: { /* 语法薄弱点 */ },
//   listening: { /* 听力薄弱点 */ },
//   reading: { /* 阅读薄弱点 */ },
//   recommendations: [ /* 改进建议 */ ],
//   timestamp: 1727690400000
// }
```

#### 获取统计信息
```javascript
const stats = analyzer.getStats();
console.log('分析器统计:', stats);
// {
//   analysesCompleted: 25,
//   weaknessesIdentified: 87,
//   avgAnalysisTime: '156.34ms',
//   cacheHits: 12,
//   hitRate: '32.43%',
//   cacheSize: 23
// }
```

#### 清理资源
```javascript
// 清理所有缓存
analyzer.cleanup();
```

---

### 3. Performance Tracker (性能追踪器)

#### 启动追踪
```javascript
// 创建实例
const tracker = new PerformanceTracker();

// 启动追踪（使用默认配置）
tracker.startTracking();

// 或使用自定义配置
tracker.startTracking({
    samplingRate: 100,    // 采样率（毫秒）
    batchSize: 50,        // 批量大小
    bufferSize: 1000,     // 缓冲区大小
    compressionEnabled: true  // 启用压缩
});
```

#### 追踪知识掌握度
```javascript
const mastery = await tracker.trackKnowledgeMastery(learnerData, 'vocabulary_001');
console.log('知识掌握度:', mastery);
```

#### 获取实时统计
```javascript
const stats = tracker.getStats();
console.log('追踪统计:', stats);
// {
//   trackingSessions: 5,
//   dataPointsCollected: 450,
//   analysisCount: 23,
//   cacheHits: 67,
//   avgTrackingTime: '2345.67ms',
//   isTracking: true,
//   bufferSize: 12,
//   performanceCacheSize: 45,
//   trendCacheSize: 23
// }
```

#### 停止追踪
```javascript
// 停止追踪
tracker.stopTracking();

// 或停止并清理所有资源
tracker.cleanup();
```

---

## 💡 最佳实践

### 1. 性能优化

#### 利用缓存
```javascript
// ✅ 好的做法：重复分析相同数据会自动使用缓存
const analysis1 = await analyzer.analyzeWeaknesses(data);
const analysis2 = await analyzer.analyzeWeaknesses(data); // 从缓存返回

// ❌ 避免：频繁清理缓存
// analyzer.cleanup(); // 不要在每次分析后清理
```

#### 批量操作
```javascript
// ✅ 好的做法：一次性准备好所有数据
const allData = {
    vocabulary: vocabData,
    grammar: grammarData,
    listening: listeningData,
    reading: readingData
};
const analysis = await analyzer.analyzeWeaknesses(allData);

// ❌ 避免：多次调用分析方法
// await analyzer.analyzeVocabularyWeaknesses(vocabData);
// await analyzer.analyzeGrammarWeaknesses(grammarData);
// ... 这样会更慢
```

### 2. 监控和调试

#### 使用统计信息
```javascript
// 定期检查性能
setInterval(() => {
    const stats = analyzer.getStats();
    console.log('缓存命中率:', stats.hitRate);
    console.log('平均分析时间:', stats.avgAnalysisTime);
    
    // 如果缓存命中率低于30%，考虑调整缓存策略
    if (parseFloat(stats.hitRate) < 30) {
        console.warn('缓存命中率较低，可能需要优化');
    }
}, 60000); // 每分钟检查一次
```

#### 启用详细日志
```javascript
// 设置日志级别为DEBUG
if (window.logger) {
    window.logger.setLevel('DEBUG');
}

// 现在所有分析操作都会输出详细日志
const analysis = await analyzer.analyzeWeaknesses(data);
```

### 3. 内存管理

#### 定期清理
```javascript
// 在合适的时机清理资源
// 例如：用户退出学习会话
window.addEventListener('beforeunload', () => {
    analyzer.cleanup();
    tracker.cleanup();
    analytics.cleanup();
});
```

#### 监控内存使用
```javascript
// 检查缓存大小
const stats = analyzer.getStats();
if (stats.cacheSize > 40) {
    console.log('缓存接近上限，即将自动清理旧项');
}
```

---

## 🔧 常见问题

### Q1: 为什么第一次分析很慢？
**A**: 第一次分析需要执行完整的计算，没有缓存可用。后续相同数据的分析会使用缓存，速度会快很多。

### Q2: 缓存会一直增长吗？
**A**: 不会。所有模块都实现了LRU缓存机制，会自动淘汰最少使用的项。

### Q3: 如何清除所有缓存？
**A**: 调用`cleanup()`方法即可清除所有缓存。

```javascript
analyzer.cleanup();
tracker.cleanup();
analytics.cleanup();
```

### Q4: 如何查看当前系统状态？
**A**: 在浏览器控制台运行：

```javascript
// 查看所有学习分析模块状态
{
    薄弱点分析: window.weaknessAnalyzer?.getStats(),
    性能追踪: window.performanceTracker?.getStats(),
    高级分析: window.advancedAnalytics?.getStats()
}
```

### Q5: 追踪会影响性能吗？
**A**: 性能追踪经过优化，采用批量处理和异步操作，对主线程影响很小。但建议在不需要时停止追踪。

---

## 📊 性能指标说明

### Weakness Analyzer 统计

| 指标 | 说明 |
|------|------|
| `analysesCompleted` | 完成的分析次数 |
| `weaknessesIdentified` | 识别的薄弱点总数 |
| `avgAnalysisTime` | 平均分析时间（毫秒） |
| `cacheHits` | 缓存命中次数 |
| `hitRate` | 缓存命中率（%） |
| `cacheSize` | 当前缓存项数 |

### Performance Tracker 统计

| 指标 | 说明 |
|------|------|
| `trackingSessions` | 追踪会话次数 |
| `dataPointsCollected` | 收集的数据点数 |
| `analysisCount` | 分析次数 |
| `cacheHits` | 缓存命中次数 |
| `avgTrackingTime` | 平均追踪时间 |
| `isTracking` | 当前是否正在追踪 |
| `bufferSize` | 缓冲区大小 |
| `performanceCacheSize` | 性能缓存大小 |
| `trendCacheSize` | 趋势缓存大小 |

---

## 🎯 完整示例

```javascript
// 1. 初始化所有模块
const analytics = new AdvancedAnalytics();
const analyzer = new WeaknessAnalyzer();
const tracker = new PerformanceTracker();

// 2. 启动性能追踪
tracker.startTracking();

// 3. 准备学习者数据
const learnerData = {
    vocabulary: {
        knownWords: 1500,
        accuracy: 0.75,
        recentTests: [/* 测试记录 */]
    },
    grammar: {
        mastery: 0.68,
        weakPoints: ['时态', '语态']
    },
    listening: {
        score: 0.72,
        difficulty: 'medium'
    },
    reading: {
        speed: 250, // 字/分钟
        comprehension: 0.80
    }
};

// 4. 执行分析
try {
    // 分析薄弱点
    const weaknessAnalysis = await analyzer.analyzeWeaknesses(learnerData);
    console.log('薄弱点分析:', weaknessAnalysis);
    
    // 追踪知识掌握度
    const mastery = await tracker.trackKnowledgeMastery(learnerData, 'grammar_tense');
    console.log('语法时态掌握度:', mastery);
    
    // 获取所有统计
    console.log('系统统计:', {
        分析器: analyzer.getStats(),
        追踪器: tracker.getStats(),
        高级分析: analytics.getStats()
    });
    
} catch (error) {
    console.error('分析失败:', error);
}

// 5. 结束后清理资源
window.addEventListener('beforeunload', () => {
    tracker.stopTracking();
    analyzer.cleanup();
    analytics.cleanup();
});
```

---

**更新日期**: 2025年9月30日  
**版本**: 1.0  
**状态**: ✅ 已优化并测试
